# =============================================================================
# VoiceClone - Vercel Deployment (API-only, no local TTS models)
# Uses RunPod for actual TTS processing
# =============================================================================

# Core Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Database
psycopg2-binary>=2.9.9

# Authentication & Security
bcrypt>=4.0.0,<5.0.0
passlib>=1.7.4
PyJWT>=2.8.0

# LLM Integration (lightweight)
google-generativeai>=0.8.0

# Environment & Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0
python-json-logger>=2.0.0

# RunPod Client (for remote TTS)
runpod>=1.6.0
requests>=2.31.0

# Utilities
pyyaml>=6.0
